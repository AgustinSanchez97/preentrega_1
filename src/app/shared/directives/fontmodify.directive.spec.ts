import { FontmodifyDirective } from './fontmodify.directive';

describe('FontmodifyDirective', () => {
  it('should create an instance', () => {
    const directive = new FontmodifyDirective();
    expect(directive).toBeTruthy();
  });
});
